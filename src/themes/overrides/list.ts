import { Components } from "@mui/material";

export const list: Components = {
  MuiListItemButton: {
    styleOverrides: {
      root: {
        height: 42,
        borderRadius: 10,
        colorScheme: 'primary'
      },
    }
  }
}